{
  "_comment": "=== prompt_config.json TEMPLATE v2.0 === Read this entire file before filling. Keep all fields. Replace values inside <...> placeholders. Delete _comment fields in the final output.",
  "task_name": "<project_task_identifier>",
  "total_count": 0,
  "resolution": "2048x1152",
  "_resolution_comment": "2560x1440 or 2048x1152 for screenshots; 1600x1000 for diagrams; 1024x1024 for icons. Must be 16:9 for screenshot asset_type.",
  "output_dir": "./output/generated_images",
  "_output_dir_comment": "Relative to the run directory. Created automatically if missing.",
  "max_workers": 4,
  "_max_workers_comment": "Max concurrent API calls. Default 4, max 8. Single upstream only.",
  "max_retries": 3,
  "_max_retries_comment": "Retries per image on transient failure.",
  "retry_delay": 2,
  "_retry_delay_comment": "Seconds between retries.",
  "timeout": 180,
  "_timeout_comment": "API timeout in seconds per image generation call.",
  "global_prompt": "<unified environment description for all images: OS, theme, font, layout, style — do NOT describe individual image content here>",
  "_global_prompt_comment": "The global_prompt defines the SHARED visual environment. Individual image.prompt fields add per-image content on top of this. They must be semantically compatible.",
  "global_constraints": {
    "_comment": "Structured environment constraints. Each image.prompt must not violate these.",
    "asset_type": "screenshot",
    "environment": "<e.g. Windows Server / Ubuntu Desktop / macOS>",
    "ui_language": "<English / Chinese>",
    "theme": "<dark / light>",
    "style": "realistic desktop screenshot",
    "forbidden_style": ["poster", "diagram", "annotation", "callout"]
  },
  "scene_anchor": {
    "_comment": "Scene anchor for cross-image consistency. All images in the same consistency_group must share these values.",
    "id": "<numeric id, e.g. 62>",
    "hostname": "<hostname, e.g. W62-SERVER1>",
    "username": "<username, e.g. Administrator>",
    "ip_prefix": "<ip prefix, e.g. 10.21.62>",
    "path_prefix": "<path prefix, e.g. D:\\W62-DHCP>",
    "terminal": "<PowerShell / bash / cmd>"
  },
  "image_policy": {
    "_comment": "Global policy applied to all images in this batch.",
    "schema_version": "2.0",
    "default_asset_type": "screenshot",
    "default_mode": "screenshot_strict",
    "fail_on_prompt_risk": true,
    "allow_negative_forbidden_terms": true,
    "probe_retries": 3,
    "probe_timeout": 180,
    "batch_timeout": 180,
    "skip_existing_files": true,
    "ui_density": "low_information_density",
    "crop_browser_chrome": true,
    "forbid_localhost_or_dev_url": false,
    "_forbid_localhost_comment": "false = allow localhost as real app URL; only avoid fake production domains",

    "screenshot_forbidden_terms": [
      "流程图", "架构图", "讲解板", "说明面板",
      "悬浮标注", "箭头标注", "海报", "AI生成", "示意图",
      "poster", "callout", "annotation", "flowchart", "diagram"
    ],
    "diagram_forbidden_terms": [
      "海报", "讲解板", "说明面板",
      "悬浮标注", "箭头标注", "AI生成",
      "poster", "callout", "annotation"
    ],

    "quality_constraints": {
      "consistency": true,
      "pixel_sharpness": true,
      "no_blur_or_mosaic": true,
      "time_consistency": true
    },

    "required_image_fields": [
      "name", "asset_type", "mode", "resolution", "consistency_group", "prompt"
    ],
    "required_global_fields": [
      "global_prompt", "global_constraints", "scene_anchor", "image_policy", "images"
    ]
  },
  "images": [
    {
      "_comment": "=== Image entry template v2.0. Copy this block for each image. ===",
      "name": "img_001",
      "_name_comment": "Must follow img_NNN format: img_001, img_002, ... continuous numbers, no gaps.",

      "asset_type": "screenshot",
      "_asset_type_comment": "One of: screenshot, diagram, ui_mockup, photo, illustration.",

      "mode": "screenshot_strict",
      "_mode_comment": "screenshot_strict (for screenshot), diagram_strict (for diagram), generic (for others).",

      "resolution": "2560x1440",
      "_resolution_comment": "Per-image override of top-level resolution. Screenshot must be 16:9.",

      "consistency_group": "<group name, e.g. dhcp_export_w62>",
      "_consistency_group_comment": "Images sharing the same group name are checked for scene_anchor consistency.",

      "prompt": "<per-image content description — must be compatible with global_prompt and global_constraints>",

      "fixup_type": null,
      "_fixup_type_comment": "OPTIONAL. null for normal generation. Use 'clarity' for the fixed anti-blur repair prompt, or 'content' for single-point replacement fixup.",

      "reference_image": null,
      "_reference_image_comment": "OPTIONAL. Set to null for txt2img. Set to a file path for img2img fixup. When non-null, prompt should describe the DESIRED CHANGE, not the full scene."
    }
  ],
  "_images_comment": "Each entry in images[] maps to one generated image. total_count must equal the length of this array. For img2img fixup images, include reference_image; for txt2img, omit it."
}
